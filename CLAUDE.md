## Agent skills

### Issue tracker

Issues live in GitHub Issues (fishshiz/gigeo), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context layout — root `CONTEXT-MAP.md` plus per-context `CONTEXT.md`/`docs/adr/` in `apps/api`, `apps/web`, `packages/ui`. See `docs/agents/domain.md`.
